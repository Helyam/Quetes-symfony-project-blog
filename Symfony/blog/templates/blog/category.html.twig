{% extends 'base.html.twig' %}

{% block title %}All articles{% endblock %}

{% block body %}
    <h1>All Category from category's table : </h1>
    {% for i in 0..2 %}
            <div>
                <h2>{{ articles[i].id }} / {{ articles[i].title }}</h2>
                <p>{{ articles[i].content }}</p>
            </div>
        {% else %}
            Aucun article trouvé.
    {% endfor %}
    <a href="{{ path('app_index') }}">
      Back to homepage.
    </a>
{% endblock %}